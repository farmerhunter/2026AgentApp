# Data

Use this folder for public sample data only.

- `contracts/`: JSON contracts that define the demo data shapes shared by Hermes and the Web UI
- `sample_inputs/`: fake or sanitized learning materials for demos
- `sample_outputs/`: fake or sanitized generated outputs

Do not store real student data here.

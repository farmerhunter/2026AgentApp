# Local Demo Data

Place local-only demo JSON here when developing with Vite.

Frontend code should still fetch `/data/...`, which maps to this folder during local development and to `/var/www/html/data/` on the VPS.

The checked-in demo data mirrors sanitized files from `data/sample_inputs/` and `data/sample_outputs/` so the Web UI can be developed without a backend.

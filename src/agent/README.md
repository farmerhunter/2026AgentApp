# Agent

Agent modules are split by responsibility:

- `ingest/`: parse uploaded text, image, or PDF inputs
- `analysis/`: generate learning insights
- `planner/`: create and adjust study plans
- `reminders/`: create reminders and scheduled summaries
- `storage/`: read and write Markdown/JSON runtime records

# 学习故事首轮：A 批实际结果与暂停点

日期：2026-08-31。状态：A 批已实测；内容仍有需修改项，B 批和周报未运行，没有记忆被接受。不是 E5 Final Gate。

## 1. 做了什么

- 用户授权制作两份带批改试卷，并执行两批分析/记忆/周报验证。
- 复用 `../run_probe.py`，在新建私有目录运行一个包含 A06/A09/A10 的隔离 CLI job。没有修改 VPS 共享 checkout、既有 profile 配置、tmux 或产品数据库。
- `studyv2` 当前 Provider/模型为 DeepSeek / `deepseek-v4-pro`；Hermes commit 为 `ca25dc81a1720951e867f970d6686ec02ab3371e`。实际 Skill 内容与 hash 见[运行快照](results/a/skill-snapshot.md)和[manifest](results/a/manifest.json)。
- 输入来自已核对的合成文字，不是 OCR 输出；三题学生作答与文字稿逐项匹配。没有传入教师批注、评分、标准答案、人工预期或历史记忆。
- 实际耗时 71.26 秒，退出码 0，usage 记录 1 次 API 调用、8,392 tokens，估算费用 USD 0.005694585；这是 Hermes 的估算，不是账单核验。

原始记录：[输入](results/a/Q1/input.json)、[输出](results/a/Q1/stdout.txt)、[用量](results/a/Q1/usage.json)、[配置](results/a/config-snapshot.json)、[隔离检查](results/a/Q1/preflight.stdout.txt)。只归档这些合成/最小记录，不上传 stderr、Hermes home 或凭证。

## 2. 程序检查

```bash
node experiments/hermes-quality/learning-story/check-story.mjs experiments/hermes-quality/learning-story/results/a
```

通过：输入快照、Skill hash、空工具、关闭 ambient memory、三题逐题覆盖、类型/候选状态/知识与历史引用。三题均无历史引用，is_recurring=false；三条记忆候选均为 pending，没有接受操作。程序检查不证明数学和教学质量。

## 3. 内容检查（同一助手自查，非独立复核）

| 题目 | 数学与证据 | 判断与帮助 | 本轮结论 |
| --- | --- | --- | --- |
| A06 | 本题计算得到 5√2 正确，定位到直接相加被开方数也有实际步骤支持；但将 `√a＋√b 不等于 √(a＋b)` 无条件表述，遗漏零值情形 | 局部分类与可见错误基本一致，没有虚构历史；一般化句子需要限定范围 | 需修改，不能记作整批质量通过 |
| A09 | 正确识别同乘共轭式的设置，错误定位到分母把差写成和；正确结果 √5＋√2 | 保留做对的部分，未直接断言完全不会有理化，建议对准平方差关系 | 在本次输入上基本符合要求 |
| A10 | k＝2 正确，缺少求 b 的步骤，正确表达式 y＝2x＋2、x＝4 时 y＝10 | 没编造学生“认为 b＝0”的心理过程，帮助是补齐待定系数步骤；解释仍较基础，不靠新增追问来扩展 | 在本次输入上基本符合要求 |

知识引用均存在于指定地图，所选节点与题目相关；引用合法不等于已证明最佳映射。三个候选均是实际模型输出，没有人工改写成更好的版本。

### A06 的具体问题与严重程度

反例是 a＝0、b＝9：`√0＋√9＝3＝√(0＋9)`。因此把含变量的“不等于”作为无条件数学关系并不严格；在本题两个正的被开方数上，运算结果及局部判断没有错。

这是此前已出现的一般化风险，不是整道题求解失败，也不是必须推倒工程接线的理由。可以说“一般不能把根式相加写成被开方数相加”，但本轮**不代改原始输出**，也不据此宣布修订已经通过。按预先记录的停止条件，先保存问题，不自动增加提示词规则和调用。

## 4. 未做的部分与接续

- 未接受任何记忆，没有创建伪造的 accepted 状态或产品 ID。
- B 批和周报未调用，不能声称完成历史复用、重复问题识别或累积周报验证。
- 没有 OCR/切题、产品数据库持久化、页面和打印链路验证。图片进度另见[图片生成记录](../two-batch-learning-fixture/image-prompts.md)。
- 下一步需由用户决定如何处理该一般化问题及是否继续本轮；若继续，再展示需要接受的实际候选并取得明确决定，而不是默认把 A06/A10 预填为历史。无需重写总体设计，也不必等待这些内容问题才开始 David 的确定性工程工作。

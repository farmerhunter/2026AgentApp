# 周报 2.0 三次真实生成对比

**生成日期：** 2026-09-11（Asia/Shanghai）  
**输入范围：** A/B/C 三份数学练习，8 道已确认错题  
**生成方式：** 同一份输入连续调用真实 Hermes 三次  
**结果：** 三次均通过程序校验和人工内容检查

这份文档只整理学生和教师在周报页面能够看到的内容。完整规范化 JSON 保存在本地验收目录 `weekly-report-v2-qa/postfix2-generations.json`；模型分析过程和私有运行目录不进入仓库。

## 快速对比

| 项目 | 第一次 | 第二次 | 第三次 |
| --- | --- | --- | --- |
| 总览标题 | 负数加法连续出错，根式化简已见进步 | 负数加法反复出错，二次根式有进步 | 负数加法反复出错，二次根式已有进步 |
| 第一重点 | 负数加法连续出错 | 负数加法反复出错 | 异号两数相加反复出错 |
| 第二重点 | 二次根式已会先化简 | 二次根式化简有进步 | 二次根式加法已会先化简 |
| 仍需观察 | 平方差符号待确认 | 平方差公式符号待观察 | 平方差公式符号待观察 |
| 行动数 | 2 | 2 | 2 |
| 生成耗时 | 约 149 秒 | 约 161 秒 | 约 196 秒 |
| 程序校验 | 通过 | 通过 | 通过 |
| 人工检查 | 通过 | 通过 | 通过 |

三次措辞不同，但都把“异号相加重复错误”放在最高优先级，把“二次根式已会先化简”写成局部进步，并把缺少过程的平方差题保留为待观察项。没有把局部变化扩大成完全掌握。

## 第一次生成

### 本周总览

**负数加法连续出错，根式化简已见进步**

本周在不同题中负数加正数的最后一步连续算错；二次根式已从根号内直接相加改进为先化简再合并，只剩系数相加这一小步待巩固。

### 本周最值得关注

1. **负数加法连续出错**
   - 不同题的最后一步都把负数加正数误当同号相加：`-4+3` 写成 `-7`、`-6+2` 写成 `-8`。异号相加应取绝对值较大数的符号、用大减小，正确是 `-1` 和 `-4`。
   - 意义：同一错误在不同题重复出现，需先纠正。
   - 依据：B 卷第 10 题、C 卷第 7 题。
2. **二次根式已会先化简**
   - 根式加法从之前把根号内数直接相加，改进为先化简再合并（`√48=4√3`、`√75=5√3`）。但最后合并系数相加仍出错，`4+5` 写成 `8`，应为 `9√3`。
   - 意义：关键一步已改对，只差最后口算。
   - 判断边界：仅化简一步有改善，不算已完全掌握。
   - 依据：A 卷第 6 题、B 卷第 6 题、C 卷第 6 题。

### 仍需观察

**平方差符号待确认**：有一题只写了最终答案 `-3`、没有过程，正确应为 `3`；另有一道分母有理化把平方差写成 `5+2`（应为 `5-2`），疑似同一符号问题，但缺过程无法确认。

### 下周行动

1. 专项练负数加法：复习异号相加规则，练习 `-4+3`、`-6+2`，完成标准是能一步算对 `-6+2=-4`。
2. 核对根式合并系数：只加根号外系数、根号内不变，重算 `4√3+5√3=9√3`。

## 第二次生成

### 本周总览

**负数加法反复出错，二次根式有进步**

最需关注的是负数加法，两道一次函数求值都在最后一步算错；二次根式则从根号内直接相加，进步到能先化简再合并。

### 本周最值得关注

1. **负数加法反复出错**
   - 两道一次函数求值都在最后一步出错：`-4+3` 算成 `-7`、`-6+2` 算成 `-8`，都把异号相加当成同号相加取负。
   - 意义：解析式都求对了，却栽在最后一步求值，直接丢分。
   - 判断边界：两次错误模式相同，但本周仅这两道题可见。
   - 依据：B 卷第 10 题、C 卷第 7 题。
2. **二次根式化简有进步**
   - 两次把根号内数直接相加，最近一次已会先化成最简、再合并同类二次根式，只差最后系数 `4+5` 算成了 `8`。
   - 意义：方法已经走对，离全对只差最后一步。
   - 判断边界：最近一次系数相加仍出错，还需巩固。
   - 依据：A 卷第 6 题、B 卷第 6 题、C 卷第 6 题。

### 仍需观察

**平方差公式符号待观察**：分母有理化时平方差展开写成 `5+2=7`（应为 `5-2=3`），另有一题答案差一个符号但无过程。因为缺少中间过程，暂不能确定是否为同一问题。

### 下周行动

1. 练熟异号两数相加：练习 `-4+3`、`-6+2` 等异号相加，代入求值后单独核对最后一步符号。
2. 巩固二次根式系数相加：重做先化简再合并的同类题，合并时只加根号外系数，根号内不变。

## 第三次生成

### 本周总览

**负数加法反复出错，二次根式已有进步**

一次函数求值最后一步异号相加反复算错；二次根式加法已从直接相加根号内数，改为先化简再合并，仅系数相加仍出错。

### 本周最值得关注

1. **异号两数相加反复出错**
   - 一次函数求值中，不同题目的最后一步异号相加都出错：`-4+3` 写成 `-7`、`-6+2` 写成 `-8`。解析式和代入步骤都对，只在最后一步把异号相加当成了绝对值相加。
   - 意义：影响函数求值结果，是有理数运算的基础。
   - 判断边界：仅在代入求值最后一步出现，未见独立的有理数运算题。
   - 依据：B 卷第 10 题、C 卷第 7 题。
2. **二次根式加法已会先化简**
   - 前两题把根号内数字直接相加；到 `√48+√75` 时已能先化成 `4√3、5√3` 再合并，方向对了，只是系数相加 `4+5` 写成 `8`，应为 `9√3`。
   - 意义：关键概念已纠正，只剩合并系数口算。
   - 判断边界：只在一道题中见到正确化简，系数相加仍会出错。
   - 依据：A 卷第 6 题、B 卷第 6 题、C 卷第 6 题。

### 仍需观察

**平方差公式符号待观察**：分母有理化时平方差展开符号出过错（`5-2` 写成 `5+2`）；另有一道平方差题只写答案 `-3`、无过程，错因无法定位。

### 下周行动

1. 专项练异号两数相加：先判断两数符号与绝对值，再用大绝对值减小绝对值并取对应符号；完成标准是正确算出 `-6+2=-4`、`-4+3=-1`。
2. 巩固二次根式加减合并：先化成最简二次根式，只合并根号外系数并核对；完成标准是 `√48+√75=9√3`。

## A/B/C 人工检查结论

- A、B 卷两道根式题确实都存在“直接相加根号内数字”的错误，C 卷对应题已会先化简，但系数相加仍错；三次周报均准确表达了这段变化。
- B、C 卷两道一次函数题的解析式和代入步骤正确，错误都发生在最后一步异号相加；三次周报均未把整个一次函数知识点判断为不会。
- B 卷平方差题只有最终答案，三次周报都保留“错因不确定”，没有编造过程。
- 三次周报中的题目关系、学生作答、正确答案、数学算式、证据关联和行动建议均经人工核对无误。
- 三次结果均未触发一票否决项。

# weekly-learning-report 2.0 真实回放记录

**日期：** 2026-09-11（Asia/Shanghai）

**目的：** 检查周报 2.0 的真实 Hermes 输出能否同时通过程序校验和人工内容验收。原始模型 stdout 含模型分析过程，只保存在 VPS 私有目录，不提交仓库；本文记录脱敏范围、hash、失败原因和人工结论。

## 环境与输入

- Hermes CLI：VPS `/home/ubuntu/.hermes/hermes-agent/venv/bin/hermes`
- profile 来源：`/home/ubuntu/.hermes/profiles/studyv2`
- Provider / model：DeepSeek / `deepseek-v4-pro`
- 最终周报调用参数：`--max-turns 1 --reasoning low`
- 私有证据目录：`/opt/hermes/2026agentapp-private/dev/weekly-v2-20260911-codex`
- 最终 Skill SHA-256：`5c19dbc8f3588fcfdad394d8a1b6d3a8f084ea93b2b391dfe73eaa9f145e0e66`
- A/B 输入 SHA-256：`e33acb92c13018e217eadef98b7d5e2c8c784a669c83159f4527f2920bf2191`
- A/B/C 输入 SHA-256：`2944a3dbdb29b72bc97a03a5c1a772c4a18e450f37d43cf0da907dd542aefbb6`
- A/B/C 数据来源：VPS 私有 `showcase-abc` 快照的只读副本；实际范围为 3 份练习、8 道已确认错题。

输入均为项目已有的原创仿真练习和真实保存的 findings，不包含人工预期结论。测试没有修改生产数据库、当前快照、Nginx、systemd 或公开页面。

## 运行结果

| 回放 | 推理级别 | 结果 | 得到的改进 |
| --- | --- | --- | --- |
| A/B 1 | profile 默认 | 失败：2.0 前的解析器误选了嵌套小对象；最终正文也过长 | 解析器改为优先选择带领域 contract 的完整 JSON |
| A/B 2 | profile 默认 | 失败：正文 814 字 | 收紧首页内容选择和各区块长度 |
| A/B 3 | profile 默认 | 失败：551 字，但行动引用未显示证据 | 行动引用必须来自某一个已展示重点或观察项 |
| A/B 4 | profile 默认 | 失败：300 秒超时 | 删除诱发逐字计算的提示 |
| A/B 5 | profile 默认 | 失败：正文 810 字 | 周报任务单独改用 low reasoning |
| A/B 6 | low | **程序与人工通过**：652 字，约 90 秒 | 重复问题 E1/E4、局部改善 E3/E6、证据不足 E5、两项行动均成立 |
| A/B/C 1 | low | 程序通过：668 字；**人工不通过** | 找到“唯一跨题重复”这一错误范围断言，并加入 Skill 禁令 |
| A/B/C 2 | low | **程序不通过**：824 字且行动过长 | 保存前校验正确拒绝；按有界回放约定停止追加调用 |

## A/B 人工复核

- A06 与 B06 使用两道不同题目的实际步骤支持二次根式加法重复问题。
- A10 与 B10 只说明“求截距 b”这一步有改善，并保留 B10 的负数加法错误。
- B09 只有最终答案，报告明确说明无法确认负号来自哪一步。
- A09 没有抢占重点，只与 B09 一起作为平方差符号的待观察项；B09 仍保留不确定限制。
- 两项行动都有步骤、完成检查和依据；没有正确率、成绩趋势、整体掌握、干预效果、内部 ID 或实验术语。
- 数学核对通过：`√18＋√8＝5√2`、`√27＋√12＝5√3`、`−4＋3＝−1`、平方差结果为 `3`。

## A/B/C 人工复核

第一份 A/B/C 输出正确识别了两条新变化：B10/C 卷对应题都在异号加法最后一步出错；C 卷根式题已经会先化简，但把 `4＋5` 算成 `8`。它没有把局部改善扩大成完全掌握，只有最终答案的 B09 也保持不确定。

该输出把负数加法称为“本周唯一跨题重复”，忽略了 A/B 根式错误也曾跨题重复，因此人工否决。第二份修订输出避开了这句话，但生成 3 个重点并超过首页硬长度，程序拒绝保存。当前结论是：

- 确定性工程链路、安全保存和旧报告保护通过；
- A/B 真实内容通过；
- A/B/C 的核心证据识别基本稳定，但尚无一份同时通过程序和人工的最终输出；
- #115 继续保持进行中，不能宣称完整内容 Final Gate 已通过。

## PR #116 隔离部署首轮稳定性复测

提交 `a04e172` 部署在 VPS 私有目录，使用独立 API/Web 端口和 `showcase-abc` 快照副本；正式数据库、服务和 Nginx 未改动。当前自然周无数据流程返回 `no_data`，历史报告数量与内容 hash 不变。第一次 A/B/C 生成约 105 秒，得到 2 个重点、1 个观察项和 2 个行动，通过程序与人工复核；第二次因首个重点可见正文 179 字超过 160 字被拒绝；第三次因第二个重点标题 19 字超过 18 字被拒绝。两次失败前后通过版报告 hash 相同。

页面实测同时发现：1366px 桌面版正常，768px 与 390px 被历史摘要的 `nowrap` 最小宽度撑开；A4 打印为 2 页，但观察卡片在页间拆分。本轮据此增加网格项 `min-width: 0`、打印卡片防跨页，以及低于硬门禁的生成目标预算和安全分句收短。修订后的连续三次真实生成、窄屏和打印结果仍需另行记录，完成前不更新 Final Gate 结论。

## 可复核 hash

- A/B 通过版原始 stdout：`9c12c4fa21ea2b1d2f9dc7a5500b3a5a7f4b7a88535a166aae6b5e53e37b35ab`
- A/B 提取后模型 JSON：`7ba0842f071e6d8c4616570b5c418871bdd3e51cac82490a3eeed9cc3b8dba8`
- A/B 服务端规范化 JSON：`475d8ba1697048059e5bf046ddeb87928322641eb41a877793a3d55596d9cdae`
- A/B/C 首次原始 stdout：`a68a1e210288ff6cdf2e4af237004b766c61c446a2d992bb9013808c406938d7`
- A/B/C 首次提取后模型 JSON：`0bed13c67c4c410eba09fb1bc78a406e50b43bc4a9a369a1ba6660b26d213958`
- A/B/C 首次服务端规范化 JSON：`a3077d9c6e52bf5ce1b24f39f9072b8f51147c5e44adc7755c6006f520b3fe7d`
